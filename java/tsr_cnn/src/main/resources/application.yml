# 服务器配置
server:
  port: 8081
  servlet:
    context-path: /

# 数据库配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tsr_cnn?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 3524369163Li
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  # JPA 配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# 图像存储路径
app:
  image:
    storage-path: uploads/
  
  # 模型配置
  model:
    path: classpath:models/tsr_cnn.onnx
